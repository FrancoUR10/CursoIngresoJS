function mostrar()
{
    var contador = 0
    var clave = prompt("Ingrese la clave.");
    while (clave != "utn750" && contador >= 3);
    {
        clave = prompt();
    }


}//FIN DE LA FUNCIÓN
